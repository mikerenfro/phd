%%%
%%% Save this file as JOBNAME-packages-loaded-after-hyperref.sty, where
%%% JOBNAME is the base filename for your main thesis/dissertation .tex 
%%% file.
%%%

% Use this file to add packages that should be loaded *after* hyperref.
% See [1-3] for a list of packages that have weird interactions with
% hyperref, and should be loaded either before or after it. See [4-5] for
% a list of packages that should be loaded in a particular order, but
% don't necessarily involve hyperref.
%
% Packages that have no particular incompatibility with others can be
% loaded in either file.
%
% [1] http://mirrors.ctan.org/macros/latex/contrib/hyperref/README
% [2] http://tex.stackexchange.com/questions/4515/whats-the-right-order-when-loading-packages
% [3] http://tex.stackexchange.com/questions/1863/which-packages-should-be-loaded-after-hyperref-instead-of-before
% [4] http://tex.stackexchange.com/questions/3090/packages-that-need-to-be-included-in-a-specific-order
% [5] http://www.macfreek.nl/mindmaster/LaTeX_package_conflicts

%%% If you'll be using the \lstinputlisting{} command or the
%%% lstlisting environment to include source code, you can customize
%%% its appearance here.

%\lstset{ %
%  language=Matlab,           % choose the default language of the code
%  basicstyle=\footnotesize,  % font size are used for the code
%  showstringspaces=false,    % underline spaces within strings
%  numbers=left,              % where to put the line-numbers
%  numberstyle=\footnotesize, % font size used for the line-numbers
%  stepnumber=5,              % the step between two line-numbers
%  numbersep=5pt,             % how far the line-numbers are from the code
%  showspaces=false,          % show spaces within strings with symbols
%  breaklines=true,           % automatically break long lines
%  showtabs=false             % show tabs within strings with symbols
%}

\lstset{language=Python,
  stringstyle=\color{blue},
  commentstyle=\color{red},
  showstringspaces=false,
  frame=lines}
\lstdefinelanguage{APDL}{comment=[l]!}
\lstdefinelanguage{Abaqus}{comment=[l]**}

%\usepackage[backend=bibtex]{biblatex}

\usepackage{cleveref} % for cross-referencing without needing to add ``Figure'', ``Table'', etc.

% https://tex.stackexchange.com/a/177033/3345
\usepackage[noend]{algpseudocode}
\algrenewcommand{\algorithmiccomment}[1]{\hfill{\color{red}$\triangleright$ #1}}
\algnewcommand{\algorithmicand}{\textbf{ and }}
\algnewcommand{\algorithmicor}{\textbf{ or }}
\algnewcommand{\OR}{\algorithmicor}
\algnewcommand{\AND}{\algorithmicand}
\makeatletter
\newcounter{algorithmicH}% New algorithmic-like hyperref counter
\let\oldalgorithmic\algorithmic
\renewcommand{\algorithmic}{%
  \stepcounter{algorithmicH}% Step counter
  \oldalgorithmic}% Do what was always done with algorithmic environment
\renewcommand{\theHALG@line}{ALG@line.\thealgorithmicH.\arabic{ALG@line}}
\makeatother

% https://tex.stackexchange.com/a/16060/3345
\MakeRobust{\Call}
